{
  "name": "energy-state-analyzer",
  "displayName": "Energy State Analyzer",
  "description": "Visualize code energy states in real-time (Python, F#, TypeScript, Kotlin, C++)",
  "version": "0.13.0",
  "publisher": "cardamom-code",
  "license": "MIT",
  "icon": "images/icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/cardamomcode/energy-state-analyzer.git"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other",
    "Linters",
    "Visualization"
  ],
  "keywords": [
    "python",
    "fsharp",
    "typescript",
    "kotlin",
    "c++",
    "code quality",
    "cyclomatic complexity",
    "cognitive complexity",
    "static analysis",
    "code smells",
    "refactoring"
  ],
  "activationEvents": [
    "onLanguage:python",
    "onLanguage:fsharp",
    "onLanguage:typescript",
    "onLanguage:kotlin",
    "onLanguage:cpp"
  ],
  "main": "./dist/extension.js",
  "bin": {
    "energy-state-analyzer": "./dist/cli.js",
    "energy-state-cli": "./dist/cli.js"
  },
  "contributes": {
    "commands": [
      {
        "command": "energy-state-analyzer.analyze",
        "title": "Analyze Energy State"
      }
    ],
    "configuration": {
      "title": "Energy State Analyzer",
      "properties": {
        "energyStateAnalyzer.includeTestFiles": {
          "type": "boolean",
          "default": false,
          "description": "Also flag magic numbers and magic strings in test files (test_*.py, *_test.py, *Test.py, *.test.ts, etc.). Off by default because test code is full of intentional literals."
        },
        "energyStateAnalyzer.nesting.mediumThreshold": {
          "type": "number",
          "default": 3,
          "minimum": 0,
          "description": "Nesting depth above which control-flow blocks (if/for/while/with) are flagged as medium energy."
        },
        "energyStateAnalyzer.nesting.highThreshold": {
          "type": "number",
          "default": 5,
          "minimum": 0,
          "description": "Nesting depth above which control-flow blocks are flagged as high energy instead of medium."
        },
        "energyStateAnalyzer.cyclomaticComplexity.mediumThreshold": {
          "type": "number",
          "default": 10,
          "minimum": 1,
          "description": "Cyclomatic complexity above which a function is flagged as medium energy."
        },
        "energyStateAnalyzer.cyclomaticComplexity.highThreshold": {
          "type": "number",
          "default": 15,
          "minimum": 1,
          "description": "Cyclomatic complexity above which a function is flagged as high energy instead of medium."
        },
        "energyStateAnalyzer.cognitiveComplexity.mediumThreshold": {
          "type": "number",
          "default": 15,
          "minimum": 1,
          "description": "Cognitive complexity above which a function is flagged as medium energy."
        },
        "energyStateAnalyzer.cognitiveComplexity.highThreshold": {
          "type": "number",
          "default": 25,
          "minimum": 1,
          "description": "Cognitive complexity above which a function is flagged as high energy instead of medium."
        },
        "energyStateAnalyzer.coherence.largeFunctionLines": {
          "type": "number",
          "default": 20,
          "minimum": 1,
          "description": "Line count above which a function counts as 'large' for file coherence purposes."
        },
        "energyStateAnalyzer.coherence.maxLargeFunctions": {
          "type": "number",
          "default": 5,
          "minimum": 1,
          "description": "Number of large functions (see largeFunctionLines) a file can contain before it's flagged for coherence."
        },
        "energyStateAnalyzer.coherence.singleDomainNameShare": {
          "type": "number",
          "default": 0.7,
          "minimum": 0,
          "maximum": 1,
          "description": "Share of a file's functions that must share a leading name word (e.g. extractFoo/extractBar) to treat the file as one coherent domain and skip the function-count sprawl check."
        },
        "energyStateAnalyzer.coherence.maxTypeDiversityRatio": {
          "type": "number",
          "default": 0.4,
          "minimum": 0,
          "maximum": 1,
          "description": "Maximum ratio of distinct parameter/return base types to typed functions for a file to be treated as one type-cohesive module and skip the function-count sprawl check. A stronger signal than singleDomainNameShare when there's enough type-annotation coverage to trust it."
        },
        "energyStateAnalyzer.coherence.minTypedCoverage": {
          "type": "number",
          "default": 0.5,
          "minimum": 0,
          "maximum": 1,
          "description": "Minimum share of a file's functions that must carry an explicit parameter or return-type annotation before typeCohesionShare is trusted; below this the detector falls back to singleDomainNameShare."
        },
        "energyStateAnalyzer.magicNumber.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether to flag magic numbers."
        },
        "energyStateAnalyzer.magicNumber.allowlist": {
          "type": "array",
          "items": {
            "type": "number"
          },
          "default": [
            0,
            1,
            -1,
            2
          ],
          "description": "Additional numeric literals to exempt alongside the built-in structural values 0, 1, -1, and 2."
        },
        "energyStateAnalyzer.magicString.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether to flag magic strings (string literals compared, checked for membership, or used as a dict/object key)."
        },
        "energyStateAnalyzer.magicString.minDuplicates": {
          "type": "number",
          "default": 2,
          "minimum": 1,
          "description": "Number of times the same string literal must recur at a decision point before it's flagged as a magic string."
        },
        "energyStateAnalyzer.magicString.allowlist": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "",
            "utf-8",
            "__main__"
          ],
          "description": "String literals that are never flagged as magic strings, regardless of context."
        },
        "energyStateAnalyzer.includeFixtures": {
          "type": "boolean",
          "default": false,
          "description": "Analyze files that .esaignore excludes when opened in the editor, for visually verifying detector fixtures. Does not affect the CLI/CI scan, which always honors .esaignore."
        },
        "energyStateAnalyzer.matchOpportunity.minBranches": {
          "type": "number",
          "default": 3,
          "minimum": 2,
          "description": "Number of branches (if + elif/else-if) an if/elif chain must have, all keyed on the same variable, before it's flagged as a match/switch opportunity."
        },
        "energyStateAnalyzer.nesting.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether to flag deeply nested control-flow blocks (nesting detector). Thresholds are set in .esaconfig.json."
        },
        "energyStateAnalyzer.cyclomaticComplexity.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether to flag functions by cyclomatic complexity. Thresholds are set in .esaconfig.json."
        },
        "energyStateAnalyzer.cognitiveComplexity.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether to flag functions by cognitive complexity. Thresholds are set in .esaconfig.json."
        },
        "energyStateAnalyzer.coherence.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether to flag file coherence problems (function-count sprawl, god classes). Thresholds are set in .esaconfig.json."
        },
        "energyStateAnalyzer.parameterCount.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether to flag functions with too many parameters. Thresholds are set in .esaconfig.json."
        },
        "energyStateAnalyzer.primitiveObsession.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether to flag primitive obsession (too many primitive arguments or returns in a function)."
        },
        "energyStateAnalyzer.opaqueBoolean.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether to flag opaque booleans/literals used directly in conditionals instead of a named constant."
        },
        "energyStateAnalyzer.logicalControlFlow.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether to flag repeated or inverted boolean expressions across adjacent control-flow branches (logical control flow)."
        },
        "energyStateAnalyzer.inversion.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether to flag inversion opportunities where a condition's structure could be simplified by inverting it."
        },
        "energyStateAnalyzer.errorShadowing.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Whether to flag functions whose error handling (try/catch/except) occupies most of the body, shadowing the business logic it wraps."
        },
        "energyStateAnalyzer.errorShadowing.threshold": {
          "type": "number",
          "default": 0.5,
          "minimum": 0,
          "maximum": 1,
          "description": "Share of a function's statements inside error-handling regions at which it is flagged (medium). Tunable in .esaconfig.json as errorShadowing.threshold."
        },
        "energyStateAnalyzer.errorShadowing.highThreshold": {
          "type": "number",
          "default": 0.7,
          "minimum": 0,
          "maximum": 1,
          "description": "Share at which the finding is high severity. Tunable in .esaconfig.json as errorShadowing.highThreshold."
        },
        "energyStateAnalyzer.errorShadowing.minNamedNodes": {
          "type": "number",
          "default": 8,
          "minimum": 1,
          "description": "Minimum number of statements a function must have before error shadowing is considered, so tiny wrappers don't trip the rule. Tunable in .esaconfig.json as errorShadowing.minNamedNodes."
        },
        "energyStateAnalyzer.colors.highEnergy": {
          "type": "string",
          "default": "#fb8500",
          "description": "Color for high-energy violations (background tint, gutter icon, and complexity heatmap). Defaults to a deep amber so it reads as 'hot' rather than duplicating the red used for compiler/lint errors."
        },
        "energyStateAnalyzer.colors.mediumEnergy": {
          "type": "string",
          "default": "#ffb703",
          "description": "Color for medium-energy violations (background tint and gutter icon)."
        },
        "energyStateAnalyzer.colors.lowEnergy": {
          "type": "string",
          "default": "#99dd99",
          "description": "Color for low-energy violations (background tint and gutter icon)."
        },
        "energyStateAnalyzer.colors.backgroundOpacity": {
          "type": "number",
          "default": 0.1,
          "minimum": 0,
          "maximum": 1,
          "description": "Opacity of the background tint applied behind flagged lines."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "prepublishOnly": "npm run package",
    "fable": "rm -rf fable-out && dotnet fable src/EnergyState.fsproj --lang javascript --outDir fable-out/extension --noCache && dotnet fable cli/EnergyState.Cli.fsproj --lang javascript --outDir fable-out/cli --noCache",
    "fable-tests": "rm -rf fable-tests && dotnet fable tests/EnergyState.Tests.fsproj --lang javascript --outDir fable-tests --noCache && echo '{\"type\":\"module\"}' > fable-tests/package.json",
    "compile": "npm run fable && webpack",
    "watch": "npm run compile && (dotnet fable watch src/EnergyState.fsproj --lang javascript --outDir fable-out/extension --noCache --runWatch npx webpack & dotnet fable watch cli/EnergyState.Cli.fsproj --lang javascript --outDir fable-out/cli --noCache)",
    "package": "npm run fable && webpack --mode production --devtool hidden-source-map",
    "lint": "dotnet fantomas src tests cli --check",
    "format": "dotnet fantomas src tests cli",
    "format-check": "dotnet fantomas src tests cli --check",
    "md-lint": "markdownlint .",
    "test": "npm run fable-tests && node fable-tests/Main.js",
    "analyze": "node dist/cli.js",
    "license-check": "license-checker --production --onlyAllow \"MIT;Apache-2.0;BSD-2-Clause;BSD-3-Clause;ISC;0BSD;CC0-1.0;Unlicense\" --excludePackages \"$npm_package_name@$npm_package_version\""
  },
  "devDependencies": {
    "@vscode/vsce": "^3.2.1",
    "copy-webpack-plugin": "^14.0.0",
    "license-checker": "^25.0.1",
    "markdownlint-cli": "^0.43.0",
    "webpack": "^5.75.0",
    "webpack-cli": "^7.2.2"
  },
  "dependencies": {
    "web-tree-sitter": "^0.26.12"
  }
}
