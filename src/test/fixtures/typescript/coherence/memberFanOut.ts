import {
    appState,
    container,
    createRequest,
    deleteRequest,
    fetchRequest,
    recordId,
    serviceToken,
    syncCursor,
    threadState,
    updateRequest
} from './api-cdf';

function doSomething(): number {
    return 1;
}
